import { CourseCertificate } from './course-certificate';

describe('CourseCertificate', () => {
  it('should create an instance', () => {
   // expect(new CourseCertificate()).toBeTruthy();
  });
});
