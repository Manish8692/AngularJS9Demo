import { ResumeWorkExperience } from './resume-work-experience';

describe('ResumeWorkExperience', () => {
  it('should create an instance', () => {
    //expect(new ResumeWorkExperience()).toBeTruthy();
  });
});
