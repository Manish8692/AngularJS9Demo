import { ResumeOrganization } from './resume-organization';

describe('ResumeOrganization', () => {
  it('should create an instance', () => {
    //expect(new ResumeOrganization()).toBeTruthy();
  });
});
